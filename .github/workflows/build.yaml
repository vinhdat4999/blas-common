name: Build and Publish to JFrog

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Install xmllint
        run: sudo apt-get install -y libxml2-utils
      - name: Extract version from pom.xml
        id: extract-version
        run: |
          version=$(xmllint --xpath "/*[local-name()='project']/*[local-name()='version']/text()" pom.xml)
          echo "::set-output name=version::$version"
      - name: Copy settings.xml
        run: |
          mkdir -p $HOME/.m2
          cp settings.xml $HOME/.m2/
      - name: Cache Maven dependencies
        uses: actions/cache@v2
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-
      - name: Build and Test with Maven
        run: mvn clean install
      - name: Publish to JFrog
        run: mvn deploy -DskipTests
#        run: |
#          curl -u ${{ secrets.JFROG_USERNAME }}:${{ secrets.JFROG_PASSWORD }} \
#            -T target/blas-common-${{ steps.extract-version.outputs.version }}.jar \
#            "https://dattest.jfrog.io/artifactory/blas-maven-libs-release/com/blas/blas-common/${{ steps.extract-version.outputs.version }}/blas-common-${{ steps.extract-version.outputs.version }}.jar" && curl -u ${{ secrets.JFROG_USERNAME }}:${{ secrets.JFROG_PASSWORD }} \
#            -T blas-common-${{ steps.extract-version.outputs.version }}.pom \
#            "https://dattest.jfrog.io/artifactory/blas-maven-libs-release/com/blas/blas-common/${{ steps.extract-version.outputs.version }}/blas-common-${{ steps.extract-version.outputs.version }}.pom"
